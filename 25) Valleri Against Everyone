#include <bits/stdc++.h>
using namespace std;
int main(){
    int t;
    cin>>t;
    
    while (t--){
        int n;
        cin>>n;
        bool flag=true;
        unordered_map<int,int> freq;
        while (n--){
            int a;
            cin>>a;
            freq[a]++;
            if (freq[a]>1){
                flag=false;
            }
        }
        if (!flag){
            cout<<"Yes"<<endl;
        }
        else{
            cout<<"No"<<endl;
        }
    }
    //T.C:- O(n)
    //S.C.:- O(1)
    return 0;
}
